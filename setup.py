from setuptools import setup

setup(name='gym_orekit',
      version='0.0.1',
      install_requires=['gym', 'thrift', 'geopandas', 'rasterio', 'descartes', 'matplotlib', 'numpy']  # And any other dependencies foo needs
)